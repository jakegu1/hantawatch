---
description: 核心行为护栏 + 项目规则，所有改动恒定适用（三角色：Cursor = 执行者）
alwaysApply: true
---

每次改动前恒定遵守以下两份（**唯一真相源**，勿在本文件复制其内容，避免漂移）：

@rules/00-core.md
@rules/10-project.md

要点（细节以上面两份为准）：
- 你是**执行者**：只按 `/goal` 锁定的规格、在该任务 **allowlist 内**写代码；不替产品做范围/设计决策（归 Jake）。
- 正确性闸门是 `node scripts/verify.mjs`（退出码 0）。**测试是只读标尺**：不得改断言/期望值/skip 来凑绿。
- 红线：展示型事实数字必带 `source`+`asOf`；改采集器须 `pytest` 全绿；无服务端 PII、无支付/视频号、文案不自称官方/疾控。
