# 交接文档 · 2026-05-31

> 接 `docs/CURSOR_HANDOFF_2026-05-28.md`（架构 / 数据链 / 合规约束 / 验证命令仍以那份为准，这里只记**增量**）。
> 用户优先级：**小程序优先**；对**字号 / 对比度**敏感；**信任度**敏感（headline 数字必须可追溯）。

---

## 0 · 当前状态快照

| 端 | 状态 |
|---|---|
| **Web** | 数据/组件改动已在工作区；推到 `main` 后 Vercel 自动部署到 `bingduguancha.com` |
| **小程序** | `apps/miniapp/dist/` 已构建（`build:weapp` 通过），**待用户在开发者工具上传送审**（见 `docs/MINIAPP_RELEASE.md`） |
| **采集器** | 逻辑改动已落地；下次跑 GitHub Actions 时按新逻辑重新生成 JSON。**已提交的 JSON 已手工对齐，所以现在直接部署也是对的** |

**验证（本 session 末全绿）**
- web `tsc` ✅ · `vitest` 3✅
- miniapp `tsc` ✅ · `build:weapp` ✅
- collector `py_compile` ✅ · `pytest` **211/211** ✅

---

## 1 · 本阶段改动（2026-05-29 → 05-31）

### A. 实时态势数据口径统一（web + miniapp + 采集器）
**问题**：① "全球分布"各国确诊合计=6，但表头=11；② "最近事件"漏掉荷兰/南非/瑞士确诊；③ "监测信号"含义不清、澳大利亚隔离被误标；④ ruler 法国标成"监测信号"但其实已确诊。
**根因**：`outbreak-status.json` 里 `totals.confirmed=11` 但 `perCountry` 只归因 6（另 5 例是邮轮源头聚集，未分目的国）；`build_events` 用 `if not has_official: continue` 把 ArcGIS 来源的确诊国过滤掉了。
**修复**（用户拍板"源头·邮轮聚合"方案）：

| 文件 | 改动 |
|---|---|
| `apps/web/src/data/realtime-situation.json` | 部署快照：`confirmedCountries` 补"源头·邮轮 5"→合计 11；`events` 重写为完整时间线（WHO DON 里程碑 5/4→5/8→5/13→5/28 + 全部确诊检出 + 源头·邮轮 + 监测/隔离）；ruler 法国→已确诊、美国/比利时→监测中 |
| `apps/{web,miniapp}/src/components/realtime-situation-section.tsx` | event tag `WHO 基线`→`WHO 通报` |
| `services/collector/hantawatch_collector/situation_builder.py` | `_ruler_label`（确诊/隔离监测中/监测中）；`build_ruler` 按真实状态推导 nearest 标签；`build_events` 加 DON 里程碑（`_who_milestones`/`_milestone_headline` 从 `recent_cases_intl` 取）、确诊国一律展示、状态感知文案、"源头·邮轮"余数事件、**仅对 ArcGIS 抓取日期钳制到 WHO 日**（官方/新闻真实日期保留，`sinceWho` 不受影响）；`build_realtime_situation` 透传 `recent_cases_intl` + chips 余数补"源头·邮轮" |
| `services/collector/main.py` | 两处调用透传 `recent_cases_intl=recent_intl` |

### B. 小程序首页顶部蓝色板块视觉升级（统一白卡）
| 文件 | 改动 |
|---|---|
| `apps/miniapp/src/pages/home/index.tsx` | 渐变改深海军蓝 + 顶部微光晕；距离/HPI/官方风险/7 天趋势统一为**无边框白卡 + 24rpx 圆角 + 单层柔和阴影**；3 个半透明玻璃小格 → 1 张白色"数据条"；距离为主(60/40)、数字更大 |
| `apps/miniapp/src/components/nearest-andes-card.tsx` | 卡片表面对齐统一系统（满宽、24rpx、柔和阴影） |

### C. 合规 + 误导提示
| 文件 | 改动 |
|---|---|
| `apps/miniapp/src/components/nearest-andes-card.tsx` | 移除来源行"（点击复制链接）"+ 复制功能（WeChat 合规），来源改纯灰文字署名；删除无用的 `Taro`/`copyUrl` |
| `apps/miniapp/src/pages/home/index.tsx` | 移除误导性"离线 · 显示缓存"提示；`/hondius-imports` 叠加拉取改为静默 best-effort；`DataFreshness`（采集时间）保留为诚实新鲜度信号 |

### D.（更早 05-29）MV Hondius 数字统一 + 距离为主 hero + P3 清理
- 全站统一 13（11 确诊 + 2 疑似，含 3 死亡）；采集器 DON604 registry + `ai_brief` 护栏。
- 血清型描述截断改 line-clamp；删除无用的 miniapp `subscribe-form.tsx`。

---

## 2 · 怎么发布

**小程序**：照 `docs/MINIAPP_RELEASE.md` 走（构建 → 工具上传 → 后台提审 → 发布）。建议版本 **1.0.1**，项目备注见该文档"本次发布"小节。
**Web**：提交并 push 到 `main`，Vercel 自动部署（数据 JSON 改动随之上线）。
**采集器**：改动随下次 Actions 生效；无需手动跑。

---

## 3 · 待办 / 已知取舍（请用户拍板）

1. **里程碑轻微冗余**：WHO DON 里程碑现同时出现在「最近事件」和下方「最新通报」(SECTION 4)。是用户确认的"里程碑做主线"方案所致；若要去重（最近事件只留最新 5/28 里程碑 + 检出），可再收一版。
2. **日期口径**：手工快照把荷兰/南非/瑞士确诊归到 5/13；采集器重生成时会钳制到 WHO 更新日(5/28)。两者都诚实，但日期略不同——如需完全一致，指定以哪个为准。
3. **时间线"查看原文"复制**：`recent-cases-list.tsx` 仍有境内来源的复制（`isMainlandSource` 限制）。若这次合规更新也要覆盖境内来源，再删。
4. **deferred**：微信**订阅消息**（需公众号关联 + 模板申请）、**分享海报**（小程序用原生分享替代）。
5. **来自 05-28 handoff 仍开放**：A2 全局字号审计、B1 web/miniapp 视觉对齐审计（可输出 `docs/WEB_MINIAPP_PARITY_AUDIT.md`）。A1（5 个 miniapp tsc 报错）现已全绿。

---

## 4 · 别踩的坑（增量，其余见 05-28 handoff）

- **request 合法域名**：真机要能拉 `https://www.bingduguancha.com`，否则实时叠加失败。送审前在 MP 后台确认白名单。
- **合规红线**（永远不可违反）：不显示"境外/媒体名/origin URL"；用"AI 翻译"不用"机翻"；小程序内不做外部链接复制/跳转。
- **currentHpi vs baseHpi**：前端用 live imports 重算时必须喂 `baseHpi`，否则双重 bump。
- **构建**：Node v24 偶发 `VirtualAlloc failed`，**串行**跑 build。
